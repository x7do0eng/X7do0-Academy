'''
========= Challenge: 1 =========
#g Ask the user for (Convert age to integer using function nesting):
    age

#g Use conditionals to print:
    If age < 13   → Child
    If age >= 13 and age < 20 → Teenager
    If age >= 20 and age < 60 → Adult
    Otherwise → Senior
=========================
''' 

#*--------------------------------

'''
========= Challenge: 2 =========
#g Ask the user for:
    number 

#g Use conditionals to check:
    If number == 0 → Print "Zero"
    If number > 0  → 
        If (number % 2 == 0) → Print "Positive Even"   
        Else → Print "Positive Odd"
    Otherwise →
        If (number % 2 == 0) → Print "Negative Even"
        Else → Print "Negative Odd"
=========================
'''
  
#*--------------------------------

'''
========= Challenge: 3 =========
#g Create variables:
    username
    password

#g Use conditionals (Use and, or, not):

    If (username == "admin") and (password == "1234"):
        Print "Login Successful"

    If (username == "admin") and (password != "1234"):
        Print "Wrong Password"

    Otherwise:
        Print "Invalid Username"
=========================
'''